---
creation_date: 2026-01-15 20:00
type: #Type/Guide
status: #Status/Active
tags: [Git, 版本控制, 最佳实践, 教程]
aliases: [Git工作流, 提交指南]
complexity: ⭐⭐⭐
---

# Git 提交方案完全指南

## 核心摘要

系统讲解 Git 仓库中各类文件的识别、分类和管理策略，帮助你建立正确的版本控制习惯，避免提交不必要的文件，保持仓库干净高效。

---

## 目录

1. [[#Git 仓库中的内容分类]]
2. [[#临时文件详解]]
3. [[#Obsidian 配置文件详解]]
4. [[#什么应该提交什么不应该提交]]
5. [[#.gitignore 配置指南]]
6. [[#本仓库的提交方案]]
7. [[#Git 提交最佳实践]]
8. [[#常见问题解答]]

---

## 1. Git 仓库中的内容分类

### 1.1 内容分类总览

Git 仓库中的文件可以分为以下几大类：

| 分类 | 说明 | 是否提交 | 示例 |
|------|------|---------|------|
| **源代码/文档** | 你创建的核心内容 | ✅ 提交 | .md, .cpp, .py |
| **配置文件** | 项目配置 | ⚠️ 看情况 | .obsidian/, .vscode/ |
| **生成文件** | 自动生成的内容 | ❌ 不提交 | .exe, .o, .pyc |
| **临时文件** | 缓存、锁文件 | ❌ 不提交 | .tmp, .lock, nul |
| **依赖包** | 第三方库 | ❌ 不提交 | node_modules/, vendor/ |
| **用户数据** | 个人偏好设置 | ❌ 不提交 | workspace.json |
| **资源文件** | 图片、音频等 | ✅ 提交 | .png, .jpg, .mp3 |
| **系统文件** | 操作系统生成 | ❌ 不提交 | .DS_Store, Thumbs.db |

---

## 2. 临时文件详解

### 2.1 什么是临时文件？

**临时文件**（Temporary Files）是程序运行过程中创建的、不需要长期保存的文件。

**特征：**
- 程序运行时创建
- 程序结束后可以删除
- 不包含重要数据
- 通常用于缓存、锁定、进程通信

### 2.2 当前仓库中的临时文件

#### 2.2.1 `tmpclaude-xxxx-cwd` 文件

```
tmpclaude-0fc4-cwd
tmpclaude-26f6-cwd
tmpclaude-761d-cwd
```

**来源：** Claude Code CLI 工具
**用途：** 记录当前工作目录（current working directory）
**内容：** 通常只包含一个路径字符串

**示例内容：**
```
C:\1.HYFStudy\OB\C++\c++
```

**是否提交：** ❌ **绝对不提交**

**原因：**
- 每次运行都可能变化
- 对其他人无用（路径不同）
- 污染仓库历史

#### 2.2.2 `nul` 文件

```
nul
```

**来源：** Windows 系统错误或脚本错误
**原因：** 通常是命令行重定向错误产生

```bash
# 错误示例（Windows）
command > nul  # 想重定向到空设备

# 正确写法
command > NUL  # 大写
command > $null  # PowerShell
```

**是否提交：** ❌ **不提交**

**处理：** 直接删除

```bash
rm nul
# 或
del nul
```

### 2.3 常见临时文件类型

| 扩展名 | 来源 | 说明 |
|--------|------|------|
| `.tmp` | 各种程序 | 通用临时文件 |
| `.temp` | 各种程序 | 同上 |
| `.lock` | 进程锁 | 防止多进程同时访问 |
| `.pid` | 进程ID | 记录进程号 |
| `.log` | 日志 | ⚠️ 看情况（开发日志可提交） |
| `.cache` | 缓存 | 缓存数据 |
| `~` 结尾 | 编辑器 | 备份文件（如 file.md~） |
| `.swp`, `.swo` | Vim | Vim 交换文件 |

---

## 3. Obsidian 配置文件详解

### 3.1 `.obsidian/` 目录结构

```
.obsidian/
├── app.json              ← 应用全局设置
├── appearance.json       ← 外观设置（主题、字体）
├── community-plugins.json ← 已安装的社区插件列表
├── core-plugins.json     ← 核心插件开关
├── workspace.json        ← 工作区状态（打开的文件、标签页）
├── hotkeys.json          ← 快捷键配置
├── graph.json            ← 关系图设置
├── plugins/              ← 插件目录
│   ├── obsidian-git/    ← Git 插件
│   ├── dataview/        ← Dataview 插件
│   └── ...
├── themes/               ← 主题文件
└── snippets/             ← CSS 代码片段
```

### 3.2 配置文件分类

#### 3.2.1 个人配置（❌ 不提交）

**特征：** 因人而异，不应该强制给别人

```
workspace.json        # 你打开的文件、窗口布局
app.json              # 个人偏好（是否显示行号、编辑器宽度等）
appearance.json       # 主题选择、字体大小、颜色
hotkeys.json          # 个人快捷键
```

**为什么不提交？**
- 每个人的工作习惯不同
- 提交后会覆盖别人的配置
- 频繁产生冲突

**例子：**
```json
// workspace.json
{
  "main": {
    "id": "abc123",
    "type": "split",
    "children": [
      // 你打开的文件列表
    ]
  },
  "lastOpenFiles": [
    "我最近打开的文件.md",
    // ...
  ]
}
```

#### 3.2.2 共享配置（⚠️ 可以提交）

**特征：** 团队协作需要统一的设置

```
community-plugins.json    # 团队使用的插件列表
core-plugins.json         # 核心功能开关
```

**例子：**
```json
// community-plugins.json
[
  "obsidian-git",      // Git 自动提交
  "dataview",          // 数据查询
  "templater-obsidian" // 模板引擎
]
```

**提交的好处：**
- 团队成员知道需要安装哪些插件
- 保证功能一致性

#### 3.2.3 插件和主题（⚠️ 看情况）

**插件目录：** `.obsidian/plugins/`

```
.obsidian/plugins/
├── obsidian-git/
│   ├── main.js          ← 插件代码
│   ├── manifest.json    ← 插件信息
│   └── data.json        ← 插件配置
└── dataview/
    └── ...
```

**是否提交？**

| 内容 | 是否提交 | 原因 |
|------|---------|------|
| 插件代码（main.js） | ❌ 不提交 | 可以通过插件市场安装 |
| 插件配置（data.json） | ⚠️ 看情况 | 如果是团队共享配置就提交 |
| 自定义插件 | ✅ 提交 | 团队自己开发的 |

**推荐做法：**
- 不提交插件代码
- 在 README.md 中列出需要安装的插件
- 只提交插件配置（如果需要）

**主题和 Snippets：**

```
.obsidian/themes/        # 主题文件
.obsidian/snippets/      # 自定义 CSS
```

**是否提交？**
- 主题：❌ 不提交（每个人喜好不同）
- Snippets：⚠️ 看情况
  - 功能性 CSS（修复布局bug）→ ✅ 提交
  - 美化 CSS（个人喜好）→ ❌ 不提交

### 3.3 当前仓库的 Obsidian 变更分析

```
modified:   .obsidian/app.json                    ← 个人设置
modified:   .obsidian/appearance.json             ← 外观设置
modified:   .obsidian/community-plugins.json      ← 插件列表（可提交）
modified:   .obsidian/plugins/obsidian-importer/data.json ← 插件配置
modified:   .obsidian/workspace.json              ← 工作区（不提交）

Untracked files:
	.obsidian/plugins/obsidian-day-planner/      ← 新插件代码
	.obsidian/plugins/obsidian-hover-editor/
	.obsidian/plugins/obsidian-minimal-settings/
	.obsidian/plugins/obsidian-style-settings/
	.obsidian/snippets/                           ← 自定义CSS
	.obsidian/themes/                             ← 主题
```

**建议：**
- ✅ 提交 `community-plugins.json`（让别人知道需要哪些插件）
- ❌ 不提交 `app.json`, `appearance.json`, `workspace.json`
- ❌ 不提交 `plugins/` 目录（插件代码）
- ⚠️ 提交 `snippets/` 如果有功能性 CSS
- ❌ 不提交 `themes/`

---

## 4. 什么应该提交，什么不应该提交

### 4.1 ✅ 应该提交的内容

#### 4.1.1 核心内容文件

```
✅ 笔记文件
c++/C++到汇编视角_完整学习笔记.md
工具/Claude/Skill.md
OneNote/UE5/C++/Actor.md

✅ 资源文件
pic/Exported image 20260113112223-0.png
pic/Pasted image 20260114174948.png

✅ 配置文件（项目级别）
.gitignore
README.md
CLAUDE.md
```

#### 4.1.2 文档类型

| 文件类型 | 是否提交 | 说明 |
|---------|---------|------|
| .md | ✅ | Markdown 笔记 |
| .txt | ✅ | 文本文档 |
| .pdf | ⚠️ | 文档（注意大小） |
| .docx | ⚠️ | Word（推荐转成 .md） |

#### 4.1.3 图片和媒体

| 文件类型 | 是否提交 | 注意事项 |
|---------|---------|---------|
| .png, .jpg | ✅ | 笔记配图 |
| .gif | ✅ | 动画图 |
| .svg | ✅ | 矢量图（推荐） |
| .psd | ❌ | Photoshop 源文件（太大） |
| .mp4 | ⚠️ | 视频（考虑使用外链） |

**图片优化建议：**
```bash
# 压缩 PNG
pngquant --quality=65-80 input.png -o output.png

# 压缩 JPG
jpegoptim --max=85 input.jpg

# 转换为 WebP（更小）
cwebp -q 80 input.png -o output.webp
```

### 4.2 ❌ 不应该提交的内容

#### 4.2.1 临时文件

```
❌ 编辑器临时文件
~file.md
file.md.swp
.DS_Store
Thumbs.db

❌ 编译产物
*.exe
*.o
*.obj
*.pyc
__pycache__/

❌ 日志和缓存
*.log
*.cache
.temp/
.tmp/

❌ 进程文件
*.pid
*.lock
tmpclaude-*
nul
```

#### 4.2.2 大文件

```
❌ 大型二进制文件
data.db (数据库文件)
large-video.mp4 (大视频)
model.pth (机器学习模型)
```

**建议：**
- 使用 Git LFS（Large File Storage）
- 或使用外部存储（云盘）+ 链接

#### 4.2.3 敏感信息

```
❌ 密钥和密码
.env
credentials.json
api_keys.txt
config_prod.yaml (包含密码的生产配置)

❌ 个人信息
private-notes.md (个人日记)
salary.xlsx (工资表)
```

**如何处理敏感信息：**
```bash
# 1. 使用环境变量
# .env.example (提交这个)
API_KEY=your_api_key_here
DB_PASSWORD=your_password

# .env (不提交，放在 .gitignore)
API_KEY=sk-real-key-12345
DB_PASSWORD=real_password_123

# 2. 使用加密工具
git-crypt
git-secret
```

#### 4.2.4 依赖包

```
❌ Node.js
node_modules/

❌ Python
venv/
.venv/
site-packages/

❌ C++
build/
out/
.vs/
```

**为什么不提交？**
- 体积巨大（node_modules 可达数百MB）
- 可以通过包管理器重新安装
- 不同平台可能不兼容

**正确做法：**
```bash
# 提交依赖清单
package.json        # Node.js
requirements.txt    # Python
Cargo.toml          # Rust
pom.xml             # Java

# 别人安装
npm install         # Node.js
pip install -r requirements.txt  # Python
cargo build         # Rust
```

---

## 5. `.gitignore` 配置指南

### 5.1 什么是 `.gitignore`？

`.gitignore` 是一个特殊文件，用于告诉 Git **哪些文件不需要跟踪**。

### 5.2 基本语法

```gitignore
# 注释（以 # 开头）

# 忽略特定文件
temp.txt
debug.log

# 忽略所有 .tmp 文件
*.tmp

# 忽略整个目录
temp/
cache/

# 不忽略特定文件（例外规则）
!important.log

# 忽略所有 .log，但不忽略 error.log
*.log
!error.log

# 忽略特定目录下的文件
build/*.exe

# 递归忽略所有子目录中的 .tmp
**/*.tmp
```

### 5.3 针对本仓库的 `.gitignore`

我为你生成了一个适合你的 `.gitignore`（见本文末尾）。

### 5.4 如何测试 `.gitignore`

```bash
# 查看哪些文件会被忽略
git status --ignored

# 检查特定文件是否被忽略
git check-ignore -v temp.txt

# 查看某个文件为什么被忽略
git check-ignore -v path/to/file
```

### 5.5 已经提交的文件如何忽略？

如果文件已经被 Git 跟踪，添加到 `.gitignore` 是无效的。

**解决方法：**

```bash
# 1. 从 Git 中移除（但保留本地文件）
git rm --cached file.txt
git rm --cached -r folder/

# 2. 提交移除操作
git commit -m "Remove tracked files that should be ignored"

# 3. 添加到 .gitignore
echo "file.txt" >> .gitignore
git add .gitignore
git commit -m "Update .gitignore"
```

---

## 6. 本仓库的提交方案

### 6.1 文件分类结果

根据你的仓库情况，我已经分析了所有文件：

#### ✅ 应该提交的核心内容

```
# 新增的重要内容
工具/Claude/Skill.md                                    ← Claude Code 完整教程
工具/Claude/Obsidian笔记整理Skill.md                     ← Obsidian 自动化
工具/Claude/Skill快速入门.md                             ← 5分钟上手
工具/Claude/Cowork.md                                   ← 协作工作流
c++/C++到汇编视角_完整学习笔记.md                        ← C++汇编教程

# UE5 笔记重组
OneNote/UE5/C++/*.md                                    ← UE5 C++ 笔记
OneNote/UE5/材质/*.md
OneNote/UE5/灯光/*.md

# 重新组织的笔记
c++/CDO/*.md                                            ← CDO 相关笔记移动
UE/*.md                                                 ← UE 笔记重组

# 图片资源
pic/Exported image 20260113*.png                        ← 大量截图
pic/Pasted image *.png                                  ← 粘贴的图片

# 配置文件更新
工具/Git.md                                              ← Git 使用笔记更新
c++/c++汇编/Const 1.md                                   ← 汇编笔记更新
```

#### ⚠️ 需要处理的文件

```
# Obsidian 配置（部分提交）
.obsidian/community-plugins.json                        ← 插件列表（提交）
.obsidian/app.json                                      ← 个人设置（不提交）
.obsidian/appearance.json                               ← 外观（不提交）
.obsidian/workspace.json                                ← 工作区（不提交）

# 插件目录（不提交代码，只提交配置）
.obsidian/plugins/*/data.json                           ← 插件配置（选择性提交）
```

#### ❌ 应该忽略的内容

```
# 临时文件
tmpclaude-0fc4-cwd
tmpclaude-26f6-cwd
tmpclaude-761d-cwd
nul

# 个人偏好
.obsidian/workspace.json
.obsidian/app.json
.obsidian/appearance.json

# 插件代码
.obsidian/plugins/*/main.js
.obsidian/plugins/*/manifest.json

# 主题和 Snippets
.obsidian/themes/
.obsidian/snippets/

# 每日暂存（你标注了"大模型不要读取"）
每日暂存（大模型不要读取）/
```

### 6.2 具体操作步骤

#### 步骤 1：清理临时文件

```bash
# 删除临时文件
rm tmpclaude-*
rm nul

# 或使用 Git 清理未跟踪的文件（⚠️ 危险操作，先预览）
git clean -n  # 预览会删除什么
git clean -f  # 真正删除
```

#### 步骤 2：创建/更新 `.gitignore`

```bash
# 检查是否已有 .gitignore
ls -la | grep gitignore

# 如果没有，创建它
# 内容见本文末尾的 .gitignore 模板
```

#### 步骤 3：移除不应该跟踪的文件

```bash
# 移除 Obsidian 个人配置（保留本地文件）
git rm --cached .obsidian/app.json
git rm --cached .obsidian/appearance.json
git rm --cached .obsidian/workspace.json

# 移除插件代码
git rm --cached -r .obsidian/plugins/

# 移除主题和 Snippets
git rm --cached -r .obsidian/themes/
git rm --cached -r .obsidian/snippets/
```

#### 步骤 4：添加核心内容

```bash
# 添加新创建的文档
git add "工具/Claude/"
git add "c++/C++到汇编视角_完整学习笔记.md"
git add "Git提交方案.md"

# 添加重组的笔记
git add "OneNote/UE5/"
git add "c++/CDO/"
git add "UE/"

# 添加图片资源
git add "pic/"

# 添加更新的配置
git add ".obsidian/community-plugins.json"
```

#### 步骤 5：提交变更

```bash
# 查看将要提交的内容
git status

# 提交
git commit -m "$(cat <<'EOF'
📚 重大更新：知识库重组与 Claude Code 集成

## 新增内容
- ✨ Claude Code Skill 完整教程体系（4个核心文档）
  - Skill.md：完整概念指南
  - Obsidian笔记整理Skill.md：4个实用 Skill 套件
  - Skill快速入门.md：5分钟上手教程
  - Cowork.md：高效协作工作流

- ✨ C++ 到汇编视角完整学习笔记（1000+ 行深度教程）
  - 整合 8 个原始笔记文件
  - 完整的汇编代码分析
  - 实战练习和调试技巧

## 笔记重组
- 📁 UE5 笔记结构化：OneNote/UE5/ 目录重组
- 📁 C++ CDO 笔记独立：c++/CDO/ 目录建立
- 📁 UE 通用笔记迁移：UE/ 目录创建

## 图片资源
- 🖼️ 新增 180+ 导出图片（汇编代码截图）
- 🖼️ Obsidian 粘贴图片整理

## 配置更新
- 🔧 Git 使用笔记更新
- 🔧 Obsidian 插件列表更新

## 清理工作
- 🗑️ 移除待整理目录的旧笔记
- 🗑️ 清理重复和过时内容
- 🗑️ 移除 Obsidian 个人配置文件

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
EOF
)"
```

#### 步骤 6：推送到远程

```bash
# 查看远程仓库
git remote -v

# 推送到远程（假设远程名为 origin，分支为 master）
git push origin master

# 如果需要强制推送（⚠️ 慎用）
# git push -f origin master
```

### 6.3 提交信息编写规范

#### 格式模板

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型

| 类型 | 说明 | Emoji |
|------|------|-------|
| feat | 新功能 | ✨ |
| fix | Bug 修复 | 🐛 |
| docs | 文档更新 | 📚 |
| style | 格式调整 | 💄 |
| refactor | 重构 | ♻️ |
| perf | 性能优化 | ⚡ |
| test | 测试 | ✅ |
| chore | 构建/工具 | 🔧 |
| revert | 回滚 | ⏪ |

#### 示例

```bash
# 简单提交
git commit -m "docs: 添加 C++ 汇编学习笔记"

# 详细提交
git commit -m "$(cat <<'EOF'
✨ feat: 添加 Claude Code Skill 完整教程

- 创建 4 个核心教程文档
- 包含从概念到实践的完整路径
- 提供 Obsidian 自动化 Skill 套件

Closes #123
EOF
)"
```

---

## 7. Git 提交最佳实践

### 7.1 提交频率

**原则：** 小步快跑，频繁提交

```
✅ 好的做法：
- 完成一个功能 → 提交
- 修复一个 bug → 提交
- 重构一部分代码 → 提交

❌ 不好的做法：
- 一周写了很多代码，最后一次提交
- 提交信息：修改了一些东西
```

**建议频率：**
- 每天至少 1-2 次提交
- 每个独立功能 1 次提交
- 重大更改前先提交（保存进度）

### 7.2 提交粒度

**原则：** 一次提交只做一件事

```
✅ 好的提交：
Commit 1: docs: 添加 C++ 汇编笔记
Commit 2: feat: 添加 Claude Skill 教程
Commit 3: refactor: 重组 UE5 笔记目录

❌ 不好的提交：
Commit 1: 添加笔记、修复bug、更新配置、重构代码
```

### 7.3 提交信息规范

**原则：** 清晰、简洁、有意义

```
✅ 好的提交信息：
feat: 添加 C++ 虚函数的汇编分析
fix: 修复图片链接错误
docs: 更新 Git 使用指南

❌ 不好的提交信息：
更新
修改了一些东西
WIP（Work In Progress）
```

### 7.4 分支管理策略

#### 7.4.1 简单项目（个人学习）

```
master (main)  ← 只有一个主分支
```

**适用场景：**
- 个人笔记仓库
- 学习项目
- 小型文档库

#### 7.4.2 Git Flow（团队协作）

```
master         ← 生产环境
  ↑
develop        ← 开发分支
  ↑
feature/*      ← 功能分支
hotfix/*       ← 紧急修复
```

**示例：**
```bash
# 创建功能分支
git checkout -b feature/claude-skill-docs

# 完成后合并回 develop
git checkout develop
git merge feature/claude-skill-docs

# 删除功能分支
git branch -d feature/claude-skill-docs
```

### 7.5 避免常见错误

#### 错误 1：提交了临时文件

```bash
# 解决方法
git rm --cached temp.txt
echo "temp.txt" >> .gitignore
git commit -m "chore: 移除临时文件并更新 .gitignore"
```

#### 错误 2：提交信息写错了

```bash
# 修改最后一次提交信息
git commit --amend -m "正确的提交信息"

# ⚠️ 如果已经推送，需要强制推送
git push -f origin master
```

#### 错误 3：提交了敏感信息（密码、密钥）

```bash
# ⚠️ 严重问题！需要彻底清理历史

# 方法 1：使用 filter-branch（复杂）
git filter-branch --tree-filter 'rm -f secrets.txt' HEAD

# 方法 2：使用 BFG Repo Cleaner（推荐）
bfg --delete-files secrets.txt
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# 方法 3：重新开始（如果历史不重要）
rm -rf .git
git init
git add .
git commit -m "Initial commit"
```

#### 错误 4：合并冲突

```bash
# 1. 拉取最新代码
git pull origin master

# 2. 如果有冲突，手动解决
# 编辑冲突文件，删除标记符号：
#   <<<<<<< HEAD
#   your changes
#   =======
#   remote changes
#   >>>>>>> remote-branch

# 3. 标记为已解决
git add conflict-file.md

# 4. 完成合并
git commit
```

---

## 8. 常见问题解答

### Q1: 临时文件删除后还能恢复吗？

**A:** 如果文件从未被 Git 跟踪，删除后无法恢复。建议：
- 使用回收站而不是 `rm`
- 定期备份重要数据

### Q2: `.gitignore` 不生效怎么办？

**A:** 可能是文件已经被跟踪了。

```bash
# 1. 查看文件是否被跟踪
git ls-files | grep filename

# 2. 如果存在，先移除
git rm --cached filename

# 3. 提交移除操作
git commit -m "chore: 移除不应跟踪的文件"
```

### Q3: 提交了大文件，推送失败怎么办？

**A:** GitHub 限制单个文件 100MB。

```bash
# 方法 1：移除大文件
git rm --cached large-file.bin
git commit --amend

# 方法 2：使用 Git LFS
git lfs install
git lfs track "*.bin"
git add .gitattributes
git add large-file.bin
git commit -m "Add large file with LFS"
```

### Q4: 如何查看某个文件的提交历史？

```bash
# 查看文件的提交历史
git log -- path/to/file.md

# 查看文件的修改内容
git log -p -- path/to/file.md

# 查看谁修改了哪一行
git blame path/to/file.md
```

### Q5: 如何回滚到之前的版本？

```bash
# 1. 查看提交历史
git log --oneline

# 2. 回滚到指定提交（保留工作区修改）
git reset --soft <commit-hash>

# 3. 回滚到指定提交（丢弃工作区修改）
git reset --hard <commit-hash>

# 4. 创建新提交来撤销之前的提交
git revert <commit-hash>
```

### Q6: Obsidian 配置文件冲突怎么办？

**A:** 永远选择保留本地配置。

```bash
# 遇到冲突时，使用本地版本
git checkout --ours .obsidian/workspace.json

# 或使用远程版本（不推荐）
git checkout --theirs .obsidian/workspace.json

# 更好的做法：不提交这些文件
git rm --cached .obsidian/workspace.json
echo ".obsidian/workspace.json" >> .gitignore
```

### Q7: 如何同步多台电脑的 Obsidian 配置？

**推荐方案：**

```bash
# 方案 1：不同步配置，各自独立
# .gitignore 包含所有 .obsidian/

# 方案 2：只同步插件列表
# .gitignore
.obsidian/*
!.obsidian/community-plugins.json

# 方案 3：使用符号链接
# 将配置文件链接到其他位置
ln -s ~/Dropbox/obsidian-config/.obsidian .obsidian
```

---

## 附录：完整 `.gitignore` 模板

### 针对本仓库的 `.gitignore`

```gitignore
# ===================================
# Git 忽略文件配置
# 项目：Obsidian 学习笔记（C++ & UE5）
# 更新：2026-01-15
# ===================================

# ===================================
# 1. 临时文件和缓存
# ===================================

# Claude Code 临时文件
tmpclaude-*

# Windows 系统文件
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
nul

# macOS 系统文件
.DS_Store
.AppleDouble
.LSOverride
._*

# Linux 系统文件
*~
.directory

# 编辑器临时文件
*.swp
*.swo
*.swn
*.bak
*.tmp
*~

# ===================================
# 2. Obsidian 配置（个人偏好）
# ===================================

# 工作区状态（每个人打开的文件不同）
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# 个人设置
.obsidian/app.json
.obsidian/appearance.json
.obsidian/hotkeys.json

# 图形设置（个人偏好）
.obsidian/graph.json

# 插件数据（大部分是个人配置）
.obsidian/plugins/*/data.json

# 例外：如果某些插件配置需要共享，可以取消注释
# !.obsidian/plugins/dataview/data.json
# !.obsidian/plugins/templater-obsidian/data.json

# ===================================
# 3. Obsidian 插件和主题
# ===================================

# 插件代码（可以通过社区插件市场安装）
.obsidian/plugins/*/
# 保留插件列表
!.obsidian/plugins/

# 主题文件（每个人喜好不同）
.obsidian/themes/

# CSS 代码片段（如果是个人美化）
.obsidian/snippets/
# 如果有功能性 CSS 需要共享，可以单独添加
# !.obsidian/snippets/fix-*.css

# ===================================
# 4. 大文件和二进制文件
# ===================================

# 视频文件（建议使用外链）
*.mp4
*.avi
*.mov
*.mkv

# 音频文件（大于 10MB 的）
*.wav
*.flac

# 压缩包
*.zip
*.rar
*.7z
*.tar.gz

# 数据库文件
*.db
*.sqlite
*.sqlite3

# ===================================
# 5. 开发相关
# ===================================

# Node.js
node_modules/
npm-debug.log
yarn-error.log

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
ENV/

# C++
*.exe
*.out
*.app
*.o
*.obj
*.so
*.dll
*.lib
*.a
build/
out/
bin/
.vs/
.vscode/

# ===================================
# 6. 日志和调试文件
# ===================================

# 日志文件
*.log
logs/
debug.log

# 调试目录
.debug/
debug/

# ===================================
# 7. 特定目录（你标注的）
# ===================================

# 每日暂存（大模型不要读取）
每日暂存（大模型不要读取）/

# 草稿和待整理
草稿/
待整理/
temp/
tmp/

# ===================================
# 8. IDE 和编辑器配置
# ===================================

# Visual Studio Code
.vscode/
*.code-workspace

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates

# JetBrains IDEs
.idea/
*.iml
*.iws

# Sublime Text
*.sublime-project
*.sublime-workspace

# ===================================
# 9. 云同步冲突文件
# ===================================

# Dropbox
.dropbox
.dropbox.attr
.dropbox.cache

# OneDrive
*_OneDrive*

# iCloud
*.icloud

# 各种云盘的冲突文件
*conflicted*
*conflict*
*sync-conflict*

# ===================================
# 10. 其他
# ===================================

# 加密文件的密钥
*.key
*.pem
*.p12

# 环境变量文件
.env
.env.local
.env.*.local

# 测试覆盖率报告
coverage/
.coverage
htmlcov/

# ===================================
# 例外规则（!表示不忽略）
# ===================================

# 保留 Obsidian 插件列表
!.obsidian/community-plugins.json
!.obsidian/core-plugins.json

# 保留 Git 配置示例
!.gitignore.example

# 保留空目录（通过 .gitkeep）
!**/.gitkeep
```

### 如何使用这个 `.gitignore`

```bash
# 1. 复制上面的内容到 .gitignore 文件
code .gitignore  # 或使用其他编辑器

# 2. 检查哪些文件会被忽略
git status --ignored

# 3. 移除已经被跟踪但应该忽略的文件
git rm --cached -r .obsidian/workspace.json
git rm --cached tmpclaude-*

# 4. 提交 .gitignore
git add .gitignore
git commit -m "chore: 添加完整的 .gitignore 配置"
```

---

## 总结

### 核心原则

1. **只提交源代码和文档**
   - 笔记 (.md)
   - 资源 (图片、PDF)
   - 配置文件（项目级别）

2. **不提交生成物和临时文件**
   - 编译产物
   - 缓存和日志
   - 个人配置

3. **保护敏感信息**
   - 密钥和密码
   - 个人隐私
   - 商业机密

4. **保持仓库干净**
   - 使用 `.gitignore`
   - 定期清理
   - 小而频繁的提交

### 行动清单

- [ ] 创建 `.gitignore` 文件
- [ ] 删除临时文件（tmpclaude-*, nul）
- [ ] 移除不应跟踪的 Obsidian 配置
- [ ] 添加核心内容到暂存区
- [ ] 编写清晰的提交信息
- [ ] 提交到本地仓库
- [ ] 推送到远程仓库
- [ ] 验证提交结果

### 学习资源

- [Pro Git 中文版](https://git-scm.com/book/zh/v2)
- [Git 图解教程](https://learngitbranching.js.org/?locale=zh_CN)
- [GitHub Guides](https://guides.github.com/)
- [.gitignore 模板库](https://github.com/github/gitignore)

---

## 下一步

1. **阅读这份文档**，理解 Git 仓库管理的核心概念
2. **执行提交步骤**（按照"6.2 具体操作步骤"）
3. **建立提交习惯**（每天至少 1 次提交）
4. **学习高级特性**（分支管理、变基、交互式提交）

祝你 Git 使用愉快！🎉

\> [!caution] This page contained a drawing which was not converted.  

//************************** Const **************  
//标注Const代表无法修改  
//1.const对象只能使用const方法 比如func（const Data& data）{data.month();} 因为这个data对象是作为const传入的 只能用const声明month（）方法才能调用  
// Data类中 void month() const { m_month++; } 错误 这个const修饰函数 表示这个函数使用到的所有成员变量都不能修改 也只能调用const函数  
//2.这个const方法如果有返回指针或者引用也需要是const声明 一般month这个方法需要提供两种版本 1）正常版本 2）const版本
    
// 插播************************** 左值引用 **************https://blog.csdn.net/weixin_43808717/article/details/137657788  
// 左引用只能引用一个有地址的东西 int& d = 1; 错误  
//*int const c = a表示c这辈子就指向a  
//*int&b = a也是一样，因为int&b 本质上和 int const 没有区别  
//他们无法改变指向的内存地址了 只能改变堆内存的值  
//  
//  
//一， ********************************** using **************************************************  
// 1.using指令 using namespace std；  
// 2.using声明 using std::string;(这个声明是有作用域的) 也可以引入枚举 using enum xxx枚举  
// 3.using别名 就是起名字 比typedef清晰很多  
// 别名支持模板  
/*template \<class Txxx\>  
using myVector = std::vector\<Txxx, std::allocator\>;*/
 
//二， ********************************** 智能指针 **************************************************  
//auto_ptr被抛弃 因为 ptr1 = ptr2的时候 ptr2会为null 在一个vector\<auto_ptr\<int\>\>中会导致一个元素为null 就不托管了 （不支持数组）  
//unique_ptr  
//1.两个指针不能指向同一个资源

2.普通回复界面 需要动态计算

3.历史记录界面
 ![Exported image](Exported%20image%2020260109132308-0.png)  

问题1：语音文本框长度高度限制是多少 （如果自适应需要动态计算动画）

1.空白界面 直接上滑+渐显动画

录制结束的时候会有3种状态
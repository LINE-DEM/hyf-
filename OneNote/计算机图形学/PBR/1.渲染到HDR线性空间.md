1.渲染到HDR线性空间

数据准备：  
贴图 美术给的都是gama空间下的 如果是参与计算的 需要手动转为Linner空间

准备的贴图都是转为线性空间的 准备一张HDR的buffer 把结果渲染到上面  
然后在Present的时候需要做一个Tollmaping

init函数做初始化的数据准备  
1.几何数据  
2.环境光的积分

1.需要两个FrameBuffer（需要用32位的深度 如果用8位就是低精度0-1） 然后需要转为 SRV（shader resource View）因为最后需要做Tonlmaping
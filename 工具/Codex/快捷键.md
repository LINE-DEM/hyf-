待确认：
详细介绍codex中的参数：approval_policy，sandbox_mode，model_reasoning_effort
[sandbox_workspace_write] network_access = false
[features] shell_snapshot = true web_search_request = true

静默模式（输出JSON） codex --quiet "分析代码质量" codex --json "生成报告" --output-last-message result.txt

/review








在指定目录启动 codex --cd /path/to/project
/quit, /exit       # 退出CLI（保存工作后再用）
/new              # 开始新对话
/resume           # 恢复之前的会话
/status           # 查看当前状态（模型、审批、token使用）

/approvals # 切换审批模式

快捷键：
Tab             # 文件路径自动补全
@               # 打开文件模糊搜索
!ls             # 执行本地shell命令
Ctrl+G          # 打开编辑器编辑长提示
Esc Esc         # 编辑上一条消息
Esc (多次)      # 回退到更早的消息
Ctrl+C          # 取消当前操作

### Skill：
/skills  # 显示可用skills列表

Skill路径：
ls -la ~/.codex/skills/
cat ~/.codex/skills/create-plan/SKILL.md



1. **VS Code 联动**  
    装 **Codex Companion** 插件，侧边栏 `@codex` 直接对话，Alt+G 把选中代码扔进 CLI，无需复制。  
    
2. **会话持久化**  
    交互里随时 `/export session.json`，第二天 `/load session.json` 继续上下文，适合超长重构。